/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "media.istockphoto.com",
      "cdn.dummyjson.com",
      "robohash.org",
    ],
  },
};

export default nextConfig;
